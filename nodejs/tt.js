const now = moment();
const year = moment().year();
const month = moment().month() + 1;
const firstDay = buyer_facility_limit.firstDay;
const secondDay = buyer_facility_limit.secondDay;
const firstTime = `${year}-${month}-${firstDay}`;
const secondTime = `${year}-${month}-${secondDay}`;


moment() <= moment('2023-01-02', 'YYYY-MM-DD').toDate()

console.log(moment())
console.log(moment('2023-01-02', 'YYYY-MM-DD').toDate())

console.log(moment().year())